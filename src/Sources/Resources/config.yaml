# Server port (CLIProxyAPI runs on 8318, ThinkingProxy forwards from 8317)
port: 8318

# Bind to localhost only for security (prevents exposure on public IPs)
host: 127.0.0.1

# Amp CLI integration (management requests only - login, user, threads)
amp-upstream-url: "https://ampcode.com"
amp-restrict-management-to-localhost: true

# Management API settings
remote-management:
  allow-remote: false
  secret-key: ""  # Leave empty to disable management API

# Authentication directory (stores OAuth tokens)
auth-dir: "~/.cli-proxy-api"

# Logging configuration
debug: false
logging-to-file: false

# Usage statistics
usage-statistics-enabled: true

# Network proxy (if needed for corporate networks)
proxy-url: ""

# Retry configuration
request-retry: 3

# Timeout for upstream provider requests
# Default: 10m. Set to "0" to disable timeout.
# Important for Extended Thinking requests which may take 5-10+ minutes.
request-timeout: "10m"

# Routing: session-sticky round robin is the default. Mixed-provider routes
# balance providers equally before rotating credentials inside a provider.
routing:
  strategy: round-robin
  session-affinity: true
  session-affinity-ttl: "1h"

# Quota management
quota-exceeded:
  switch-project: true
  switch-preview-model: true

# OAuth providers for local subscriptions
# Claude, Codex (GPT), Gemini tokens in ~/.cli-proxy-api/ will be used
# No fallback - users must authenticate providers they want to use

# No API keys needed for OAuth-based providers
generative-language-api-key: []

# Ollama Cloud models. Add an Ollama Cloud API key in Settings to activate this provider.
openai-compatibility:
  - name: ollama-cloud
    display-name: Ollama Cloud
    help-text: "Ollama Cloud API - DeepSeek, Kimi, GLM, MiniMax models"
    base-url: https://ollama.com/v1
    models:
      - alias: deepseek-v4-flash
        name: deepseek-v4-flash
      - alias: kimi-k2.7-code
        name: kimi-k2.7-code
      - alias: kimi-k3
        name: kimi-k3
      - alias: deepseek-v4-pro
        name: deepseek-v4-pro
      - alias: kimi-k2.6
        name: kimi-k2.6
      - alias: glm-5.2
        name: glm-5.2
      - alias: minimax-m3
        name: minimax-m3

# OpenCode Go models. Add an OpenCode Go API key in Settings to activate this provider.
  - name: opencode-go
    display-name: OpenCode Go
    help-text: "OpenCode Go API - DeepSeek, Kimi, GLM, GPT, Grok, Qwen models"
    base-url: https://opencode.ai/zen/go/v1
    models:
      - alias: deepseek-v4-flash
        name: deepseek-v4-flash
      - alias: deepseek-v4-pro
        name: deepseek-v4-pro
      - alias: glm-5
        name: glm-5
      - alias: glm-5.1
        name: glm-5.1
      - alias: glm-5.2
        name: glm-5.2
      - alias: glm-5.3
        name: glm-5.3
      - alias: gpt-5.6-luna
        name: gpt-5.6-luna
      - alias: grok-4.5
        name: grok-4.5
      - alias: hy3
        name: hy3
      - alias: kimi-k2.5
        name: kimi-k2.5
      - alias: kimi-k2.6
        name: kimi-k2.6
      - alias: kimi-k2.7-code
        name: kimi-k2.7-code
      - alias: kimi-k3
        name: kimi-k3
      - alias: mimo-v2-omni
        name: mimo-v2-omni
      - alias: mimo-v2-pro
        name: mimo-v2-pro
      - alias: mimo-v2.5
        name: mimo-v2.5
      - alias: mimo-v2.5-pro
        name: mimo-v2.5-pro
      - alias: minimax-m2.5
        name: minimax-m2.5
      - alias: minimax-m2.7
        name: minimax-m2.7
      - alias: minimax-m3
        name: minimax-m3
      - alias: qwen3.5-plus
        name: qwen3.5-plus
      - alias: qwen3.6-plus
        name: qwen3.6-plus
      - alias: qwen3.7-max
        name: qwen3.7-max
      - alias: qwen3.7-plus
        name: qwen3.7-plus
      - alias: qwen3.8-max
        name: qwen3.8-max
